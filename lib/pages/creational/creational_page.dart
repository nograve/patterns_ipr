import 'package:flutter/material.dart';

class CreationalPage extends StatelessWidget {
  const CreationalPage({super.key});

  static const routeName = '/';

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Creational'),
    );
  }
}
